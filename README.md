This is Python module written in C. You can make shared library and use ctype.
        
####To install just run:
`python setup.py install`

####Example

`import PyCap`
                                
`PyCap.capture("tcp")`
